---
title: Toggle
description: A two-state action whose pressed state remains visible.
---

Toggle is a two-state action that can be pressed or unpressed. The packaged
Primitive owns pressed state, activation, focus, and optional Toggle Group
membership. The Recipe adds a label, spacing, and state-dependent colors.

Use [Button](/catalog/button) for an action without retained state and
[Switch](/catalog/switch) for a labeled setting row.

## Install

<CodeGroup>

```bash React
pnpm dlx shadcn@4.13.0 add @tuiparts/react/toggle
```

```bash Solid
pnpm dlx shadcn@4.13.0 add @tuiparts/solid/toggle
```

```bash Core
pnpm dlx shadcn@4.13.0 add @tuiparts/core/toggle
```

</CodeGroup>

## Use the Recipe

### React and Solid

```tsx
import { Toggle } from "./components/ui/toggle";

<Toggle
  label="Bold"
  defaultPressed
  onPressedChange={(pressed, details) => {
    console.log({ pressed, source: details.source });
  }}
/>;
```

Use `pressed` with `onPressedChange` for controlled ownership, or
`defaultPressed` for uncontrolled initial state.

### Core

```ts
import { createToggle } from "./components/ui/toggle";

const bold = createToggle(ctx, {
  label: "Bold",
  defaultPressed: true,
});

parent.add(bold);
```

The factory returns `ToggleRenderable`.

## Customize the Recipe

The copied source owns the label child tree, spacing, and pressed-state color
mapping. Edit it to add icons, variants, or another layout. Shared colors and
density come from the [Theme Recipe](/theming).

## Behavior

The package export is the single-part `Toggle`, not `Toggle.Root`. A
standalone Toggle owns its pressed value. Inside a Toggle Group it requires a
unique `value` and adopts the group's array-valued selection.

Use the [Toggle Group Recipe](/catalog/toggle-group) when related Toggles
should share a single keyboard stop.

| Key | Behavior |
| --- | --- |
| `Enter` or `Return` | Toggles pressed state. |
| `Space` | Toggles pressed state. |

An uncancelled primary-pointer release and `press()` request the same change.
Disabled Toggles reject focus and activation.

For standalone and grouped state ownership, change details, and raw
composition, see the [Toggle Primitive](/primitives/toggle).

## Recipe API

<TypeTable
  type={{
    label: {
      type: "string",
      required: true,
      description: "Text rendered inside the Toggle.",
    },
    pressed: {
      type: "boolean",
      description: "Controlled pressed value for a standalone Toggle.",
    },
    defaultPressed: {
      type: "boolean",
      description: "Initial uncontrolled pressed value.",
    },
    onPressedChange: {
      type: "(pressed: boolean, details: ToggleChangeDetails) => void",
      description: "Called with the requested value and immutable activation source.",
    },
    disabled: {
      type: "boolean",
      description: "Prevents focus and activation.",
    },
  }}
/>

React and Solid extend `TogglePrimitive.Props` except for `children`. Core
`ToggleOptions` exposes the fields above through `createToggle()`.

## Build with the Primitive

Use the packaged [Toggle Primitive](/primitives/toggle) when you want to own
the pressed-state presentation rather than start from the labeled Recipe.
