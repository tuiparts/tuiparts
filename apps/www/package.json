{
  "name": "www",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "blume dev",
    "sync:registry": "node ../../scripts/build-registry.mjs --output apps/www/public/r",
    "build": "pnpm sync:registry && blume build",
    "preview": "blume preview",
    "deploy": "pnpm build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@orama/orama": "^3.1.18",
    "@shikijs/twoslash": "^4.2.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^7.0.2",
    "blume": "^1.0.4"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
